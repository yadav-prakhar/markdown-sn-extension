{
  "name": "markdown-sn-extension-tests",
  "version": "1.1.3",
  "description": "Test suite for Markdown to ServiceNow Chrome Extension",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "prepare": "bash scripts/install-hooks.sh"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "@vitest/ui": "^2.1.0",
    "happy-dom": "^15.11.0",
    "vite": "^6.0.3",
    "vitest": "^2.1.0"
  }
}
